package Menu.radioButton;

import javax.swing.*;

public class JsonButton extends JRadioButton {
    private JRadioButton jsonButton = new JRadioButton();

    public JRadioButton buildJsonButton() {
        jsonButton.setText("validator");

        return jsonButton;
    }
}
